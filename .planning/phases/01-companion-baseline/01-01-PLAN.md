---
phase: 01-companion-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - _locales/en/messages.json
  - popup/popup.html
  - popup/popup.js
  - app/app.html
  - app/app.js
  - README.md
autonomous: true
requirements:
  - BASE-01

must_haves:
  truths:
    - "The extension presents itself as NotebookLM++ across manifest metadata, popup, app surface, and user-facing copy."
    - "The popup remains the main operational entry point for the existing import-oriented workflows."
    - "Returning users can still recognize and use the current import actions without a navigation or layout redesign."
    - "A short utilitarian continuity line makes it clear that the current import tools remain available and working."
    - "No popup or app copy teases future export functionality in this phase."
  artifacts:
    - path: "manifest.json"
      provides: "Phase 1 brand metadata while preserving current extension structure"
      contains: "\"short_name\""
    - path: "_locales/en/messages.json"
      provides: "NotebookLM++ user-facing strings for popup and app"
      contains: "\"extName\""
    - path: "popup/popup.html"
      provides: "Popup shell with light-touch NotebookLM++ framing"
      contains: "<title>"
    - path: "app/app.html"
      provides: "Internal app/settings shell with matching brand and continuity framing"
      contains: "<title>"
  key_links:
    - from: "popup/popup.html"
      to: "popup/popup.js"
      via: "existing popup workflow remains the main entry point"
      pattern: "DOMContentLoaded|init"
    - from: "app/app.html"
      to: "app/app.js"
      via: "settings/app copy aligns with the updated brand"
      pattern: "DOMContentLoaded|initialize"
    - from: "_locales/en/messages.json"
      to: "manifest.json"
      via: "localized extension name and description"
      pattern: "extName|extDescription"
---

<objective>
Apply the Phase 1 light-touch rebrand so the extension reads as `NotebookLM++` while preserving the familiar popup-first import workflow.

Purpose: This plan satisfies the user-facing continuity side of Phase 1 without introducing export UI, navigation churn, or confusing transition messaging.

Output: Updated metadata, popup/app copy, and continuity framing that keep the current import-oriented value obvious and intact.
</objective>

<execution_context>
@/home/kosya/.codex/get-shit-done/workflows/execute-plan.md
@/home/kosya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-companion-baseline/01-CONTEXT.md
@.planning/phases/01-companion-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand manifest and locale copy without changing product structure</name>
  <files>
    manifest.json
    _locales/en/messages.json
    README.md
  </files>
  <action>
    Update extension metadata and English locale strings so the product presents as `NotebookLM++` immediately, with utilitarian copy only.

    Requirements for this pass:
    - Change `manifest.json` name/short-name metadata to the new brand while keeping the existing popup, background, and content-script structure unchanged.
    - Update `_locales/en/messages.json` keys that surface the product name, app title, description, and about text so they describe the extension as a NotebookLM companion whose current import tools remain available.
    - Keep existing import terminology recognizable where it maps to real actions (`Add to Notebook`, `Bulk Import`, notebook selection, source add flows).
    - Do not add any “coming soon”, “export soon”, or transitional “formerly Add to NotebookLM” language.
    - Refresh README top-level naming and description only as needed to match the new Phase 1 positioning; avoid documenting future export behavior here.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const manifest=JSON.parse(fs.readFileSync('manifest.json','utf8')); const messages=JSON.parse(fs.readFileSync('_locales/en/messages.json','utf8')); if(!/NotebookLM\\+\\+/.test(messages.extName.message)) throw new Error('extName not rebranded'); if(!/NotebookLM\\+\\+/.test(manifest.short_name) && !/NotebookLM\\+\\+/.test(messages.extName.message)) throw new Error('brand missing'); const text=fs.readFileSync('_locales/en/messages.json','utf8'); if(/coming soon|export soon|formerly add to notebooklm/i.test(text)) throw new Error('forbidden teaser or transition copy present'); console.log('brand copy ok');"</automated>
  </verify>
  <done>
    Manifest and English locale metadata present the extension as `NotebookLM++` while preserving recognizable import-oriented action labels and excluding future-export teaser copy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh popup and app framing while preserving familiar import actions</name>
  <files>
    popup/popup.html
    popup/popup.js
    app/app.html
    app/app.js
  </files>
  <action>
    Add the Phase 1 continuity framing to the popup and internal app/settings surfaces without altering their overall layout or control flow.

    Implementation requirements:
    - Keep the current popup as the main operational entry point. Do not remove, reorder beyond light-touch polish, or visually demote the existing import controls.
    - Update visible headings, titles, and supporting text in `popup/popup.html` and `app/app.html` to use `NotebookLM++`.
    - Insert one short continuity line in a natural place on each surface that tells the user the current import tools remain available and working.
    - Keep the tone utilitarian, not marketing-heavy. Avoid roadmap hints, beta banners, and export-oriented CTAs.
    - If `popup/popup.js` or `app/app.js` need small DOM or i18n wiring changes for the new copy, make the minimum required changes only; do not change the underlying import behavior.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const popupHtml=fs.readFileSync('popup/popup.html','utf8'); const appHtml=fs.readFileSync('app/app.html','utf8'); const popupJs=fs.readFileSync('popup/popup.js','utf8'); const appJs=fs.readFileSync('app/app.js','utf8'); if(!/NotebookLM\\+\\+/.test(popupHtml+appHtml+popupJs+appJs)) throw new Error('brand missing from UI files'); if(/coming soon|export selected notes|google docs export/i.test(popupHtml+appHtml+popupJs+appJs)) throw new Error('phase 1 UI hints at export'); console.log('ui framing ok');" && node --check popup/popup.js && node --check app/app.js</automated>
  </verify>
  <done>
    Popup and app surfaces show the new brand and continuity line while preserving the familiar import-first layout and behavior.
  </done>
</task>

</tasks>
